node_modules/
dist/
__pycache__/
*.log
.env
.vite/

# Blender backup files
*.blend1

# Ad-hoc backup copies
*.bak
*.bak-*

# OS cruft
.DS_Store
Thumbs.db
# Subagent-driven dev scratch (briefs, reports, ledger, diffs)
.superpowers/

# Locally calibrated models for on-face testing (scripts/calibrate-local.mjs)
public/calibrated/

# Scratch model fixtures: merchant GLBs copied in for local verification
# (scripts/verify-raw-passthrough.mjs, ?model=). Customer assets and several MB
# each -- kept on disk, never committed.
public/models/_*.glb
